"""Signet — cross-platform desktop GUI for using a Trezor as an SSH key.

This package is an integration/GUI layer on top of romanz/trezor-agent's
``libagent`` and ``trezorlib``. It deliberately does NOT reimplement any
cryptography or the SSH-agent protocol — see docs/plan for rationale.
"""

__version__ = "0.1.0.dev0"
